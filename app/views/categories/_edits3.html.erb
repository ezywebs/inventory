<%= form_for(@category, :remote => true, :html => {class: "form-horizontal", role: "form", name: "edit-form"}) do |f| %>
        <%= f.text_field :name, class: "form-control", placeholder: "Category name", autofocus: true %>

        <%= f.submit 'Save', class: 'btn btn-success' %>

<% end %>